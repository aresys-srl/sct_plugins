Installation
============

Requirements
------------

SCT ENVISAT/ERS ASAR Format Reader plugin requires a Python version **equal or higher than 3.10**.


Install with pip
----------------

SCT ENVISAT/ERS ASAR Format Reader package and its dependencies can be installed using ``pip`` tool:

.. code-block:: bash

    $ pip install sct_asar_reader
