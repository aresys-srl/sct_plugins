Installation
============

Requirements
------------

SCT RADARSAT-2 Reader plugin requires a Python version **equal or higher than 3.10**.


Install with pip
----------------

SCT RADARSAT-2 Reader package and its dependencies can be installed using ``pip`` tool:

.. code-block:: bash

    $ pip install sct_radarsat2_reader
