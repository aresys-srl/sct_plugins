Installation
============

Requirements
------------

SCT Aresys Product Reader plugin requires a Python version **equal or higher than 3.10**.


Install with pip
----------------

SCT Aresys Product Reader package and its dependencies can be installed using ``pip`` tool:

.. code-block:: bash

    $ pip install sct_aresys_reader
