Installation
============

Requirements
------------

SCT COSMO-SkyMed Reader plugin requires a Python version **equal or higher than 3.10**.


Install with pip
----------------

SCT COSMO-SkyMed Reader package and its dependencies can be installed using ``pip`` tool:

.. code-block:: bash

    $ pip install sct_cosmo_reader
