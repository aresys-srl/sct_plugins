Installation
============

Requirements
------------

SCT Sentinel-1 Reader plugin requires a Python version **equal or higher than 3.11**.


Install with pip
----------------

SCT Sentinel-1 Reader package and its dependencies can be installed using ``pip`` tool:

.. code-block:: bash

    $ pip install sct_sentinel1_reader
