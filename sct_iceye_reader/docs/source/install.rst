Installation
============

Requirements
------------

SCT ICEYE Reader plugin requires a Python version **equal or higher than 3.11**.


Install with pip
----------------

SCT ICEYE Reader package and its dependencies can be installed using ``pip`` tool:

.. code-block:: bash

    $ pip install sct_iceye_reader
