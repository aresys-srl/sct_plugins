# SCT Plugin: EOS-04 format reader

Sar Calibration Toolbox (SCT) plugin for reading EOS-04 SAR L1-A (SLC) and L1-B (GRD) products.

Supported acquisition modes:

- **Scansar**

This package and its dependencies can be installed using ``pip`` tool:

```bash
    pip install sct_eos04_reader
```
