# SCT Plugin: Biomass Products reader

Sar Calibration Toolbox (SCT) plugin for reading Biomass SAR L1-A data.

This package and its dependencies can be installed using ``pip`` tool:

```bash
    pip install sct_biomass_reader
```
